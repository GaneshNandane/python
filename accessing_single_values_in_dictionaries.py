# accessing dictionary values using [] brackets
# using the get() method to access the values inside a dictionary 

info={'name':'karan', 'age':19, 'eligible':True}
print(info['name'])
print(info.get('eligible'))

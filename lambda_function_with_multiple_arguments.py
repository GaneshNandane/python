# normal function to calculate the product of two mumbers
def multiply (x, y):
    return x*y

# calculating the product of two numbers using normal function
print(multiply(4, 5))

# lambda (anonymous) function to calculate the product of two numbers
multiply = lambda x, y: x*y

# calculating the product of two numbers using lambda (anonymous) function 
print(multiply(5, 6))

# Iterating through each character of a string using a for loop

# Defining a string
name = "Abhishek"

# The for loop iterates over each character of the string one by one
for i in name:

    # Printing each character followed by a comma
    # end="," prevents print() from moving to a new line
    print(i, end=",")

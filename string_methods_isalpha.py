# simple program to check if all the text are alphabetic characters or not

# defining a string
str1="Welcome"

# checking if all the characters are alphabetic characters
print(str1.isalpha())

# iterating over a tuple using enumerate function with keeping track of index for each element 

# here we are defining a tuple and iterating over tuple objects using enemerate function
colors=('red', 'green', 'blue')
for index,color in enumerate(colors):
    
    # printing each index and corresponding elements 
    print(index,color)

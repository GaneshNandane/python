# checking if the tuples are mutable in python or not 
# we know that tuples in python are immutable means that after assigning a tuple we can not change it's value 

a = (23, 54, "heart")
a[0] = "attack"

# this gives us error saying 'tuple object does not support item assignment'

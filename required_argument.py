# simple program to demonstrate the use of required arguments

# defining a function with three arguments 
# Note: in the function call we need to call all three arguments to run our program 

def name(fname, mname, lname):
    
    # printing the arguments with greeting 
    print("Hello,", fname, mname, lname)

# calling a function
name("Peter", "Ego", "Qull")

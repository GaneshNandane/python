# The pop() method is used to remove and return
# an arbitrary element from a set.

# Define a set
cities = {"Tokyo", "Madrid", "Berlin", "Delhi"}

# Remove and store an arbitrary element from the set
item = cities.pop()

# Print the set after removing an element
print(cities)

# Print the element that was removed from the set
print(item)

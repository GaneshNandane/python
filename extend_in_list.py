# add a list to a list using extend method 

colors=["Voilet", "Indigo", "Blue"]
rainbow=["Green", "Yellow", "Orange", "Red"]

# it adds all element of the rainbow into colors 
colors.extend(rainbow)

# it prints the final extended list 
print(colors)
